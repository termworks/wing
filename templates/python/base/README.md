# {{PROJECT_NAME}}

Small Python starter generated by wing with the `{{builtin_flavour}}`
environment flavour.

## Environment

{{builtin_environment_description}}

Enter the Nix shell with `direnv allow` or `nix develop --impure`.

## Commands

```sh
make setup
make build
make run ARGS='--help'
make test
make verify
make release TYPE=patch
```
